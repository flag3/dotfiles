customCommands:
  - key: "C"
    command: "git cz"
    description: "commit with commitizen"
    context: "files"
    loadingText: "opening commitizen commit tool"
    output: terminal
gui:
  mouseEvents: false
  scrollHeight: 30
git:
  autoFetch: false
  diffRenderers:
    - command: delta --light --syntax-theme="Solarized (light)" --paging=never
