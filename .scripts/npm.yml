- name: create ~/.npm/lib
  file:
    state: directory
    path: ~/.npm/lib
    mode: 0700

- name: install npm packages
  npm:
    state: latest
    global: true
    name: "{{ item }}"
  loop:
    - "@trivago/prettier-plugin-sort-imports"
    - corepack
    - eas-cli
    - git-cz
    - npm-check-updates
    - prettier
    - tsx
