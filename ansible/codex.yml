- name: create codex directory
  file:
    state: directory
    path: ~/.codex
    mode: 0700

- name: touch codex config
  file:
    state: touch
    path: ~/.codex/config.toml
    access_time: preserve
    modification_time: preserve

- name: add Codex session status hooks
  codex_session_status_hooks:
