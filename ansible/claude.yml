- name: add claude plugin marketplaces
  claude_plugin_marketplaces:
    name: "{{ item }}"
  loop:
    - affaan-m/everything-claude-code
    - anthropics/claude-plugins-official
    - openai/codex-plugin-cc

- name: install claude plugins
  claude_plugins:
    name: "{{ item }}"
  loop:
    - codex@openai-codex
    - everything-claude-code@everything-claude-code
    - notion@claude-plugins-official

- name: add Claude Code session status hooks
  claude_session_status_hooks:

- name: install mattpocock skills (provides /grill-me)
  command: npx -y skills@latest add mattpocock/skills -g -y -a claude-code -a codex
