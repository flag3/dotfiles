- name: install npm packages
  npm:
    state: latest
    global: true
    name: "{{ item }}"
  loop:
    - "@github/copilot"
    - "@google/gemini-cli"
    - "@openai/codex"
    - commitizen
    - cz-git
    - npm-check-updates
    - oxfmt
    - oxlint
    - prettier
