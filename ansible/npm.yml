- name: install npm packages
  npm:
    state: latest
    global: true
    name: "{{ item }}"
  loop:
    - "@github/copilot"
    - "@openai/codex"
    - commitizen
    - cz-git
    - npm-check-updates
    - prettier
